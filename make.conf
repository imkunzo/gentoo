# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.

CFLAGS="-march=core2 -mtune=core2 -mfpmath=sse -mmmx -msse -msse2 -msse3 -mssse3 -msse4.1 -pipe -O2 -fomit-frame-pointer"
CXXFLAGS="${CFLAGS}"

# WARNING: Changing your CHOST is not something that should be done lightly.
# Please consult http://www.gentoo.org/doc/en/change-chost.xml before changing.

CHOST="x86_64-pc-linux-gnu"

MAKEOPTS="-j3"

# These are the USE flags that were used in addition to what is provided by the
# profile used for building.

USE="-gnome -ipv6 -kde -qt -qt4 acpi alsa gtk3 gudev mmx ppp sse sse2 sse3 sse4_1 ssse3 thinkpad xft"

LINGUAS="en_US"

VIDEO_CARDS="intel i965"

GENTOO_MIRRORS="http://mirrors.163.com/gentoo/ http://mirrors.sohu.com/gentoo/ http://gentoo.gg3.net/ http://ftp.twaren.net/Linux/Gentoo/ http://mirror.datapipe.net/gentoo"

# SYNC="rsync://rsync.us.gentoo.org/gentoo-portage"

#Gentoo overlay
source /var/lib/layman/make.conf

ACCEPT_LICENSE="PUEL"

USE_PYTHON="3.3 2.7"
